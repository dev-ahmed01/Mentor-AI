# Copy to .env.production.local (ignored). Do not use example strings as secrets.
DATABASE_PASSWORD=replace-with-a-unique-random-database-password
JWT_SECRET=replace-with-at-least-32-random-bytes
FRONTEND_ORIGIN=https://mentor.example.com
WEB_BIND_ADDRESS=127.0.0.1
WEB_PORT=3000
DEMO_ENABLED=false
